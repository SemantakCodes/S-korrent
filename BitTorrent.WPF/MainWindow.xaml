<Window x:Class="BitTorrent.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BitTorrent.WPF"
        xmlns:views="clr-namespace:BitTorrent.WPF.Views"
        xmlns:vm="clr-namespace:BitTorrent.WPF.ViewModels"
        xmlns:converters="clr-namespace:BitTorrent.WPF.Views"
        mc:Ignorable="d"
        Title="BitTorrent Client" Height="720" Width="1200"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=False}">

    <Window.Resources>
        <ResourceDictionary>
            <converters:BytesToStringConverter x:Key="BytesConverter"/>
            <converters:SpeedToStringConverter x:Key="SpeedConverter"/>
            <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
            <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVis"/>
            <converters:StatusColorConverter x:Key="StatusColorConverter"/>
            
            <Style TargetType="Button" x:Key="ToolbarButton">
                <Setter Property="Padding" Value="12,6"/>
                <Setter Property="Margin" Value="4,2"/>
                <Setter Property="Background" Value="#007ACC"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="3" 
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#005A9E"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#004578"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#CCCCCC"/>
                                    <Setter Property="Foreground" Value="#888888"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ListViewItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="Margin" Value="0,1"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="#F5F5F5" BorderBrush="#DDD" BorderThickness="0,0,0,1" Padding="8">
            <StackPanel Orientation="Horizontal">
                <Button Content="🧪 Create Test Torrent" Style="{StaticResource ToolbarButton}" 
                        Command="{Binding CreateTestTorrentCommand}"/>
                <Button Content="➕ Add Torrent" Style="{StaticResource ToolbarButton}" 
                        Command="{Binding OpenTorrentFileCommand}"/>
                <Button Content="📁 Set Download Folder" Style="{StaticResource ToolbarButton}" 
                        Command="{Binding ChooseDownloadPathCommand}"/>
                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="8,0"/>
                <Button Content="⏸ Pause" Style="{StaticResource ToolbarButton}" 
                        Command="{Binding PauseSelectedCommand}"
                        IsEnabled="{Binding SelectedTorrent.IsDownloading}"/>
                <Button Content="▶ Resume" Style="{StaticResource ToolbarButton}" 
                        Command="{Binding ResumeSelectedCommand}"
                        IsEnabled="{Binding SelectedTorrent, Converter={StaticResource InverseBoolToVis}}"/>
                <Button Content="🗑 Remove" Style="{StaticResource ToolbarButton}" 
                        Command="{Binding RemoveSelectedCommand}"
                        Background="#E53935"
                        IsEnabled="{Binding SelectedTorrent, Converter={x:Null}}"/>
                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="8,0"/>
                <TextBlock Text="Download: " VerticalAlignment="Center" Foreground="#666" Margin="5,0"/>
                <TextBlock Text="{Binding DownloadPath}" VerticalAlignment="Center" FontSize="12" Foreground="#333" 
                           TextTrimming="CharacterEllipsis" MaxWidth="300"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" MinWidth="250" MaxWidth="400"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Torrent List -->
            <Border Grid.Column="0" Background="White" BorderBrush="#DDD" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#FAFAFA" BorderBrush="#DDD" BorderThickness="0,0,0,1" Padding="10">
                        <TextBlock Text="Torrents" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                    </Border>

                    <ListView Grid.Row="1" ItemsSource="{Binding Torrents}" 
                              SelectedItem="{Binding SelectedTorrent}"
                              VirtualizingStackPanel.IsVirtualizing="True"
                              SelectionMode="Single">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="Margin" Value="4,2"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#E3F2FD"/>
                                        <Setter Property="BorderBrush" Value="#2196F3"/>
                                        <Setter Property="BorderThickness" Value="2,0,0,0"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F5F5F5"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Name" Width="200">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Vertical">
                                                <TextBlock Text="{Binding TorrentInfo.Name}" 
                                                           FontWeight="Medium" FontSize="12" 
                                                           TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding TorrentInfo.TotalLength, Converter={StaticResource BytesConverter}}" 
                                                           FontSize="10" Foreground="#666"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Progress" Width="150">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Grid Height="20">
                                                <ProgressBar Value="{Binding OverallProgress}" Maximum="100" Height="16"
                                                             Foreground="#2196F3" Background="#E0E0E0"/>
                                                <TextBlock Text="{Binding OverallProgress, StringFormat='{}{0:0}%'}" 
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                                           FontSize="10" FontWeight="Bold" Foreground="#333"/>
                                            </Grid>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Status" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Status}" FontSize="11"
                                                       Foreground="{Binding Status, Converter={StaticResource StatusColorConverter}}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <TextBlock Grid.Row="1" Text="No torrents added" 
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Foreground="#999" FontSize="14"
                               Visibility="{Binding Torrents.Count, Converter={StaticResource InverseBoolToVis}}"/>
                </Grid>
            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" 
                          Background="Transparent" ResizeBehavior="PreviousAndNext"/>

            <!-- Details View -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#FAFAFA" BorderBrush="#DDD" BorderThickness="0,0,0,1" Padding="10">
                    <TextBlock x:Name="DetailsHeader" Text="Select a torrent to view details" 
                               FontSize="14" FontWeight="SemiBold" Foreground="#666"/>
                </Border>

                <ContentControl Grid.Row="1" Content="{Binding SelectedTorrent}">
                    <ContentControl.Resources>
                        <DataTemplate DataType="{x:Type vm:TorrentViewModel}">
                            <views:TorrentDetailsView/>
                        </DataTemplate>
                    </ContentControl.Resources>
                </ContentControl>
            </Grid>
        </Grid>
    </Grid>
</Window>